# Custom JWT/OTP → Better Auth Migration

**Status:** 🚧 In progress (Phases 1, 2, 3-partial, 5 ✅; Phase 4 partial — trade-role endpoint landed, broader cleanup pending)
**Branch:** `migrate/better-auth` on both `ultrasooq-backend` and `ultrasooq-frontend`
**PR:** _pending_
**Reference:** [`/Users/yasseralsaidi/dev/projects/react/qitaff`](file:///Users/yasseralsaidi/dev/projects/react/qitaff) — Next.js + Prisma + PostgreSQL Better Auth integration we are mirroring at the auth-library level.

## Goal

Replace the bespoke auth stack (custom JWT, refresh tokens, OTP, hand-rolled `/user/register`, `/user/login`, `/user/forgetPassword`, etc.) with [Better Auth](https://better-auth.com) so we get:

- Standard sign-in/sign-up flows out of the box
- Sessions + refresh as a first-class concern (no more bespoke `RefreshToken` table or rotation logic)
- Email verification, password reset, OTP, magic-link, social providers via plugins
- Built-in defenses (CSRF, cookie security, brute-force lockouts) the team doesn't have to maintain
- A small, predictable client SDK on the frontend instead of `apis/requests/auth.requests.ts` + `AuthContext` + axios refresh interceptor

## Non-goals (this PR)

- Removing the legacy `User` / `MasterAccount` tables — they still hold profile/business data referenced by ~120 other models. They stay.
- Migrating every existing user immediately — see _Phase 5_ for the data-migration plan.
- Breaking session continuity for already-logged-in users — they will be asked to sign in once after deploy.

---

## Architecture decision: Better Auth lives in the NestJS backend

Two viable homes for the `auth` instance:

| Option | Pro | Con |
|---|---|---|
| Backend (NestJS, port 3000) | Colocates with Postgres + existing Prisma + SendGrid; backend stays the API source of truth; reuses CORS already wired to `:4001` | Mounting `/api/auth/*` in NestJS needs `toNodeHandler` + `app.use` glue |
| Frontend (Next.js, port 4001) | Standard Better Auth pattern (`/app/api/auth/[...all]/route.ts`); zero glue | Auth state would live in the frontend while business data lives in backend; awkward split |

→ **Backend wins.** Mount path `/api/auth/*` (note: no `/v1` prefix — the existing global prefix excludes it).

### URL surface after migration

| Layer | Old | New |
|---|---|---|
| Sign-in | `POST /api/v1/user/login` | `POST /api/auth/sign-in/email` |
| Sign-up | `POST /api/v1/user/register` + `POST /api/v1/user/registerValidateOtp` | `POST /api/auth/sign-up/email` (verification email handled by Better Auth) |
| Forgot password | `POST /api/v1/user/forgetPassword` + `verifyOtp` + `resetPassword` | `POST /api/auth/request-password-reset` + `POST /api/auth/reset-password` |
| Refresh | `POST /api/v1/auth/refresh` | n/a — Better Auth manages session sliding window |
| Logout | `POST /api/v1/auth/logout` | `POST /api/auth/sign-out` |
| Session check | `GET /api/v1/user/me` (custom) | `GET /api/auth/get-session` |

Frontend uses `authClient.signIn.email()`, `authClient.signUp.email()`, `authClient.requestPasswordReset()`, `authClient.resetPassword()`, `authClient.signOut()`, `authClient.useSession()` — no manual fetches.

---

## Schema strategy: coexistence then cleanup

Better Auth's Prisma adapter expects four core tables plus plugin-specific ones. We add them alongside the existing models, namespaced so there's no collision:

| Better Auth table | Prisma model name | DB table |
|---|---|---|
| user | `BetterAuthUser` | `better_auth_user` (via `@@map`) |
| session | `BetterAuthSession` | `better_auth_session` |
| account | `BetterAuthAccount` | `better_auth_account` |
| verification | `BetterAuthVerification` | `better_auth_verification` |

A nullable `betterAuthId String? @unique` is added to the legacy `User` table to link a legacy `User` row (which holds tradeRole, masterAccountId, customerId, stripeAccountId, …) to its new `BetterAuthUser`. Once everyone is migrated, we can either fold the legacy fields into `BetterAuthUser` via `additionalFields` or keep the join — to be decided in a follow-up PR.

### Custom user fields on `BetterAuthUser`

Added via Better Auth's `user.additionalFields` so they're typed end-to-end:

```ts
user: {
  additionalFields: {
    tradeRole: { type: ["BUYER", "FREELANCER", "COMPANY"], required: true, defaultValue: "BUYER" },
    phoneNumber: { type: "string", required: false },
    cc: { type: "string", required: false },
    legacyUserId: { type: "number", required: false },          // FK back to User.id
    legacyMasterAccountId: { type: "number", required: false }, // FK back to MasterAccount.id
  },
},
```

(Final list TBD — will mirror what the front-end forms collect at sign-up.)

---

## Email transport

Stay on SendGrid via the existing wrapper at [`src/notification/notification.service.ts`](src/notification/notification.service.ts) — already handles "trial credits exhausted → log to console" gracefully. Three new senders to wire:

- `emailVerification.sendVerificationEmail({ user, url })` → reuse mailService HTML template
- `emailAndPassword.sendResetPassword({ user, url, token })` → reuse sendOtp template (swap "OTP" copy for a clickable link)
- `emailOTP.sendVerificationOTP({ email, otp, type })` (if we keep an OTP code path for parity with current UX) → existing OTP HTML template

Switch to Resend later (separate PR — see [auth.7 in the qitaff audit](#)) once we decide.

---

## Phases

### Phase 0 — Reference + plan ✅
- [x] Audit qitaff Better Auth setup
- [x] Decide architecture (backend hosts `auth`)
- [x] Branches created on both repos
- [x] This document

### Phase 1 — Backend foundation ✅
- [x] `npm i better-auth` in backend → v1.6.9
- [x] `src/auth-better/auth.ts` — `betterAuth({ database: prismaAdapter(prisma, { provider: "postgresql" }), … })`
- [x] Add `BetterAuthUser`, `BetterAuthSession`, `BetterAuthAccount`, `BetterAuthVerification` to `prisma/schema.prisma` (mapped to `better_auth_*` table names)
- [ ] _(deferred to Phase 5)_ `betterAuthId` link on legacy `User` — not needed yet, the link runs through `BetterAuthUser.legacyUserId`
- [x] `npx prisma db push` to add the four new tables (migrate dev refused without resetting; db push is additive)
- [x] Mount handler in `src/main.ts` via `app.use('/api/auth', toNodeHandler(auth))` BEFORE `express.json()`
- [x] Wire SendGrid sender callbacks (`sendVerificationEmail`, `sendResetPassword`) via `src/auth-better/mailer.ts` reusing the dev-fallback wrapper
- [x] Add `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `CORS_ORIGINS` to `.env`
- [x] Health-check: `curl /api/auth/get-session` returns 200 (`null`) with no session
- [x] **Smoke test passing:** `POST /api/auth/sign-up/email` creates row in `better_auth_user` and returns `{token, user}`; `POST /api/auth/sign-in/email` issues `ultrasooq.session_token` cookie + 7-day expiry; `GET /api/auth/get-session` returns full session+user (incl. custom fields `tradeRole`, `phoneNumber`, `cc`, `legacyUserId`, `legacyMasterAccountId`); wrong-password yields 401 `INVALID_EMAIL_OR_PASSWORD`; verification email is dispatched via `sendVerificationEmail` → SendGrid → falls back to `[DEV-MAIL]` console log (trial-exhausted account)

### Phase 2 — Frontend foundation ✅
- [x] `npm i better-auth --legacy-peer-deps` in frontend → v1.6.9
- [x] `lib/auth-client.ts` — `createAuthClient({ baseURL: process.env.NEXT_PUBLIC_BETTER_AUTH_URL || "http://localhost:3000" })` with placeholder `fetchOptions.onError`
- [x] Exports: `signIn`, `signUp`, `signOut`, `useSession`, `getSession`, `requestPasswordReset` _(renamed from `forgetPassword` in v1.6)_, `resetPassword`, `sendVerificationEmail`
- [x] `NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000` appended to `.env.local`
- [x] Stub note added to top of frontend `MIGRATION_TODO.mdx` pointing to this tracker

### Phase 3 — Page-by-page rewrite (partial ✅)
- [x] `app/login/page.tsx` → `signIn.email({ email, password, rememberMe })` with `onSuccess`/`onError` callbacks; Google button still wired to legacy next-auth flow (defer until social-provider plugin lands)
- [x] `app/forget-password/page.tsx` → `requestPasswordReset({ email, redirectTo: "/reset-password" })` (renamed from `forgetPassword` in Better Auth v1.6); routes back to `/login` after dispatch instead of legacy OTP page
- [x] `app/reset-password/page.tsx` → `resetPassword({ newPassword, token })` reading `?token=` from the email link; bad/missing token shows clear error state
- [x] `app/password-reset-verify/page.tsx` → replaced with a `router.replace('/forget-password')` redirect since the link-based flow has no OTP step (page kept for stale bookmarks; deletes in Phase 6)
- [x] `app/register/page.tsx` Step 1 → `signUp.email({ email, password, name, tradeRole, phoneNumber, cc })` via Better Auth additionalFields (tradeRole defaults to BUYER; user upgrades it in Step 3)
- [x] `app/register/page.tsx` Step 2 OTP verify → dropped 4-digit OTP input. Replaced with a "check your email" card + Resend button (calls `sendVerificationEmail({ email })`). Better Auth fires the verification link automatically via `emailVerification.sendOnSignUp` and the UI does NOT block on verification — user advances to Step 3 immediately.
- [x] `app/register/page.tsx` Step 3 Account Type → role cards retained (BUYER / COMPANY / FREELANCER). Submission is currently local-only with a `// TODO(better-auth-phase3): hit this new endpoint once it exists` marker. **Backend follow-up needed in Phase 4: `PATCH /api/v1/user/me/trade-role`** with body `{ tradeRole, accountName?, companyName?, companyAddress?, companyPhone?, companyWebsite?, companyTaxId? }`, auth via Better Auth session cookie (BetterAuthGuard).
- [ ] `app/otp-verify/page.tsx` → drop or keep with `emailOTP` plugin
- [ ] `context/AuthContext.tsx` → swap internals to `authClient.useSession()` while keeping the public hook shape (deferred — broad blast radius across the app)

### Phase 4 — Backend cleanup (partial ✅)
- [x] **`PATCH /api/v1/user/me/trade-role`** — `src/auth-better/auth-better.controller.ts` + `auth.guard.ts` (`BetterAuthGuard` calling `auth.api.getSession({ headers: fromNodeHeaders(req.headers) })`). `BetterAuthUser` model gained `companyName`/`companyAddress`/`companyPhone`/`companyWebsite`/`companyTaxId`/`accountName` columns (also reflected in `auth.ts` `additionalFields`). Smoke-tested: 200 with cookie, 401 without, 400 on invalid `tradeRole`.
- [ ] Delete `src/auth/` (legacy JWT controller/service/guard) — _broader cleanup not yet started; the new BetterAuthGuard only protects the trade-role endpoint so far_
- [ ] Delete custom OTP fields on `User` (`otp`, `otpValidTime`, `resetPassword`)
- [ ] Delete custom OTP fields on `MasterAccount`
- [ ] Drop `RefreshToken` table (Better Auth's `BetterAuthSession` replaces it)
- [ ] Replace `@UseGuards(JwtAuthGuard)` everywhere with the thin `BetterAuthGuard` from `src/auth-better/auth.guard.ts`

### Phase 5 — Data migration ✅
1. **Read** every legacy `User` linked to a `MasterAccount`, joined to fetch email + bcrypt password
2. **Write** one `BetterAuthUser` per `MasterAccount` with `email`, `name = firstName lastName`, `emailVerified = true` (we trust existing accounts), `legacyUserId`, `legacyMasterAccountId`
3. **Write** one `BetterAuthAccount` per user with `providerId = "credential"` and the same bcrypt hash — Better Auth's credential provider supports bcrypt out of the box
4. **Backfill** `User.betterAuthId` so existing FK relations keep working through the legacy table during the transition
5. Sessions are NOT migrated. Existing logged-in users get one forced re-login on next visit (their `ultrasooq_accessToken` cookie is invalid against Better Auth and the http interceptor will route them to `/login`).

Rollback: drop the four `better_auth_*` tables, drop the `betterAuthId` column, revert the route mounts. Legacy auth keeps working untouched.

### Phase 6 — Cleanup
- [ ] Delete `apis/requests/auth.requests.ts` (frontend)
- [ ] Delete `apis/queries/auth.queries.ts` (if it exists)
- [ ] Delete `utils/authMessages.ts` — Better Auth supplies typed error codes; we map those to translations instead
- [ ] Delete `proxy.ts` route guard middleware → replace with `await auth.api.getSession({ headers })` in a Next.js middleware
- [ ] Drop `cookies-next` if no longer used
- [ ] Drop `@types/jsonwebtoken`, `bcryptjs` if no longer used (Better Auth has its own)

---

## Plugins to enable (initial set)

Mirror qitaff but trim to what Ultrasooq actually uses today:

| Plugin | Why | Notes |
|---|---|---|
| `nextCookies()` | Cookie integration | client side, applied in handler config last |
| `bearer()` | Mobile / 3rd-party bearer-token clients (admin app) | server side |
| `emailOTP()` | Optional — keeps parity with current 4-digit OTP UX during sign-up | only if we decide not to switch to verification links |

Defer for later PRs: `username`, `passkey`, `twoFactor`, `phoneNumber`, `organization`, `admin`. Each is a small follow-up.

---

## Translation cheat sheet

| Old (custom) | New (Better Auth) |
|---|---|
| `apis/requests/auth.requests.ts → register(...)` | `authClient.signUp.email({ email, password, name, tradeRole })` |
| `apis/requests/auth.requests.ts → login(...)` | `authClient.signIn.email({ email, password, rememberMe })` |
| `apis/requests/auth.requests.ts → forgotPassword(...)` | `authClient.requestPasswordReset({ email, redirectTo })` |
| `apis/requests/auth.requests.ts → resetPassword(...)` | `authClient.resetPassword({ newPassword, token })` |
| `apis/http.ts` axios interceptor refreshing tokens | gone — Better Auth refreshes via `updateAge` sliding window automatically |
| `useAuth().user` from `AuthContext` | `authClient.useSession().data?.user` |
| `getCookie('ultrasooq_accessToken')` | gone — cookie is HTTP-only, server reads via `auth.api.getSession({ headers })` |

---

## Gotchas (from qitaff audit + ours)

- **Mount order matters.** In NestJS, `app.use('/api/auth', toNodeHandler(auth))` MUST be registered before any `bodyParser` / `ValidationPipe` global setup, otherwise Better Auth can't read raw bodies. See `src/main.ts`.
- **`baseURL` ≠ API base.** The frontend's existing `getApiUrl()` returns `http://localhost:3000/api/v1`. Better Auth client wants the host root: `http://localhost:3000`. Do NOT reuse the same env.
- **`trustedOrigins`.** Must list `http://localhost:4001` (frontend dev), production frontend, any preview-deploy patterns. Otherwise CORS rejects sign-in.
- **Bcrypt password import.** Existing passwords are bcrypt(10). Better Auth's credential provider accepts bcrypt natively — copy hash unchanged. If we ever change to argon2, we'd need a one-time rehash on next sign-in.
- **Session cookies + axios.** Once auth is on cookies, axios needs `withCredentials: true` for any `/api/v1/*` calls that run in the browser AND the backend needs `cors({ origin, credentials: true })`. Ultrasooq already has CORS — verify `credentials: true` is set.
- **Role guard.** qitaff blocks `INACTIVE`/`deletedAt` users in `databaseHooks.session.create.before`. We need the same — `User.status === 'INACTIVE'` and `MasterAccount.deletedAt != null` cases must be guarded there.
- **Frontend register flow has 3 steps.** Better Auth's `signUp.email` is one step. We keep the 3-step UX by deferring `tradeRole` + role-specific fields (`companyName`, `freelancerName`, etc.) to either `additionalFields` on signUp, or a post-signup `PATCH /api/v1/user/me` call, depending on what reads cleanly.

---

## Status updates

_Append entries as work lands; newest at top._

- **2026-05-07** — Phase 5 caveats resolved + Phase 4 partial ✅. **(a)** Migration script (`scripts/migrate-to-better-auth.ts`) now migrates orphaned MasterAccounts (no linked `User` row) with `tradeRole='BUYER'` default and `legacyUserId=null`, tallied as `migrated_orphan`. Coerces non-enum legacy `tradeRole` values (`MEMBER`, `ADMINMEMBER`, anything outside `BUYER|FREELANCER|COMPANY`) to `'BUYER'` with a `console.warn`. Re-run results: `total: 12, migrated: 0, migrated_orphan: 6, skipped_existing: 6, errors: 0`; idempotent on second run (`skipped_existing: 12`). All 12 dev MasterAccounts now in `better_auth_user`, including `ultrasooqdev@gmail.com` (verified `tradeRole=BUYER`, `legacyUserId=null`). **(b)** New `PATCH /api/v1/user/me/trade-role` lives at `src/auth-better/auth-better.controller.ts`, gated by `BetterAuthGuard` (`src/auth-better/auth.guard.ts`) which wraps `auth.api.getSession({ headers: fromNodeHeaders(req.headers) })`. `AuthBetterModule` wired into `app.module.ts`. `BetterAuthUser` schema gained six profile columns mirroring legacy `User` (`companyName`/`companyAddress`/`companyPhone`/`companyWebsite`/`companyTaxId`/`accountName`); applied via `prisma db push --accept-data-loss` (re-dropped `PerformanceMetric` + `VisitorSession` analytics tables — re-created at startup by `analytics-ingestion.service.ts`). Also fixed a latent bug: `auth.ts` was loading before Nest's ConfigModule populated `process.env`, so `DATABASE_URL` was `undefined` and Prisma silently failed with "table public.better_auth_user does not exist" — added `import 'dotenv/config'` at the top of `auth.ts`. Smoke-tested: `200` with valid cookie + `tradeRole=COMPANY`+ company fields persisted (`Acme Inc`, `123 Main St`, `+1-555-1234`); `401 No active Better Auth session` without cookie; `400` on `tradeRole=WIZARD` (`tradeRole must be one of the following values: BUYER, FREELANCER, COMPANY`). `npm run build` clean. Frontend register page Step 3 can now wire its `// TODO(better-auth-phase3)` callsite to this endpoint.
- **2026-05-07** — Phase 3 register page ✅. `app/register/page.tsx` ported to `signUp.email`. Three-step UX preserved (visual stepper unchanged); the underlying flow is now: `signUp.email()` → "check your email" card → role pick. Dropped the 4-digit OTP input — Better Auth's `sendOnSignUp` ships a verification link instead. Step 3 stages the chosen tradeRole locally with a `// TODO(better-auth-phase3)` marker pointing to a new `PATCH /api/v1/user/me/trade-role` endpoint that the backend needs in Phase 4 (body: `{ tradeRole, accountName?, companyName?, companyAddress?, companyPhone?, companyWebsite?, companyTaxId? }`, auth via BetterAuthGuard). **Verified end-to-end in browser:** register form → Better Auth user created with custom fields (`tradeRole=BUYER`, `phoneNumber`, `cc`, `name`), advanced through Step 2 (resend cooldown working) to Step 3, redirected to `/profile?fromRegister=1`, and the new credentials successfully sign in via `/login` → `/home` with the "Login Successful" toast. Also patched `proxy.ts` to allow `/profile?fromRegister=1` through without a legacy cookie — until the Phase-6 middleware swap, the legacy proxy can't see the cross-origin Better Auth session cookie (it lives on `:3000`, not `:4001`). Legacy `useRegister` / `useVerifyOtp` / `useResendOtp` hooks and manual `setCookie` writes are gone from the register page. Google OAuth still routes through legacy next-auth pending the social-provider plugin. tsc clean (only pre-existing `.next/types/validator.ts` errors unrelated to register). **Backend follow-up to track:** the `PATCH /api/v1/user/me/trade-role` endpoint above.
- **2026-05-07** — Phase 5 ✅. New script at `scripts/migrate-to-better-auth.ts` (run via `npm run migrate:better-auth`, also added). Copies `MasterAccount` + the resolved primary linked `User` (lastActiveUserId-with-isActive fallback to default-active BUYER, mirroring `user.service.ts:584`) into `BetterAuthUser`, and the bcrypt `MasterAccount.password` straight into `BetterAuthAccount.password` with `providerId="credential"` so Better Auth's credential provider authenticates legacy users without a forced reset. Wraps each MA in `prisma.$transaction` and parallelizes 5-at-a-time via `p-limit`. Idempotent: skips any MA whose email or `legacyMasterAccountId` is already in `BetterAuthUser`. **Result on dev DB:** 12 live MasterAccount rows → `total: 12, migrated: 6, skipped_existing: 0, skipped_no_primary: 6, errors: 0`. Verified `better_auth_account` rows have `providerId="credential"` and `password` prefix `$2b$10$`. Second run = `migrated: 0, skipped_existing: 6`. Bcrypt hash-format sanity check (`bcrypt.compareSync('test', firstMigratedHash)`) returned `false` cleanly (didn't throw — confirms the hash bytes are intact). **⚠️ Caveat:** 6 of 12 MasterAccount rows in dev have ZERO linked `User` rows in the `User` table at all (orphaned MasterAccounts) and were skipped — they need a Phase 6 follow-up to either backfill a User row or hard-delete the orphans before they can sign in. Not a script bug, a data state.
- **2026-05-07** — Phase 3 partial ✅. `/login`, `/forget-password`, `/reset-password` now use the Better Auth client; `/password-reset-verify` redirects to `/forget-password`. Verified end-to-end in browser: a fresh Better Auth user can sign in, redirect to `/home`, and the "Login Successful" toast fires. Register / OTP / AuthContext deferred. **⚠️ Caveat for branch consumers:** legacy users in the `MasterAccount` table can't log in on this branch yet because their bcrypt hashes haven't been migrated to `better_auth_account` — Phase 5 ships that. Existing dev/staging users should re-register or wait for the migration script.
- **2026-05-07** — Phase 1 (backend foundation) and Phase 2 (frontend foundation) ✅. Sign-up / sign-in / get-session / wrong-pwd / verification-email-dispatch all verified end-to-end via curl. Better Auth lives at `/api/auth/*` on the backend; legacy `/api/v1/user/*` untouched and still serves the live frontend. WIP PR opened — see PR header at top.
- **2026-05-07** — Branches created on both repos; this tracker checked in. qitaff audit complete; reference summary above.

## Gotchas hit during Phase 1/2

- **`prisma migrate dev` wanted a full reset** because the existing DB had drifted from migration history (3 analytics tables managed via raw SQL outside Prisma). Used `prisma db push` instead — additive, kept all data. The 3 raw-SQL tables (`ErrorLog`, `PerformanceMetric`, `VisitorSession`) get dropped because they're not in `schema.prisma`; the analytics service re-creates them on next startup via `CREATE TABLE IF NOT EXISTS`. Permanent fix: add stub Prisma models for them in a follow-up so future `db push`/`migrate` doesn't drop them again.
- **`modelName` overrides are required** when the default Better Auth model names (`user`, `session`, `account`, `verification`) collide with existing Prisma models. We have an existing `User`. Set `user: { modelName: "betterAuthUser" }`, `session: { modelName: "betterAuthSession" }`, `account: { modelName: "betterAuthAccount" }`, `verification: { modelName: "betterAuthVerification" }` at the top of `betterAuth({...})`.
- **NestJS hot reload doesn't reload `auth.ts`.** It's imported from `main.ts` (bootstrap), not from a module's DI graph. Edits to `auth-better/*.ts` need a manual restart (`preview_stop` + `preview_start`). Worth scripting later.
- **v1.6 client renamed `forgetPassword` → `requestPasswordReset`.** The audit doc/qitaff snippets still show the old name. Use the new one in pages.
- **`emailVerification.sendOnSignUp: true` is independent of `requireEmailVerification`.** We send on sign-up but DON'T require verification yet — flip both together when the verification UX is wired.
- **CORS preflight on `/api/auth/*` returns 404** if the route is mounted before `app.enableCors()`. Nest's `enableCors()` runs in the Nest layer and doesn't intercept Express `app.use(...)` mounts that come earlier in the chain. Workaround: a tiny inline middleware on `/api/auth` that responds 204 to OPTIONS with the right CORS headers, registered immediately before `toNodeHandler(auth)`. Lives in `src/main.ts`.
- **`MasterAccount.email` is non-nullable** in the schema — Prisma rejects `where: { email: { not: null } }` with `Argument 'not' must not be null`. The migration script just filters on `deletedAt: null` and trusts the schema constraint. Don't reflexively guard against null on a non-null column.
- **`tradeRole` on `BetterAuthUser` is a free-form `String?`**, but the Better Auth `additionalFields` config only declares `"BUYER" | "FREELANCER" | "COMPANY"`. ✅ Resolved 2026-05-07: the migration script now coerces `MEMBER`/`ADMINMEMBER`/anything-not-in-the-enum to `'BUYER'` (with a `console.warn`), so all freshly-migrated rows hold a valid enum value. The two pre-existing rows (`subadmin@`, `member@`) still have `ADMINMEMBER`/`MEMBER` respectively — they're skipped on re-run by the idempotency check. Re-coerce in a follow-up if needed.
- **6 of 12 dev MasterAccounts are orphaned** — no rows in `User` at all (LEFT JOIN returns null on the right). ✅ Resolved 2026-05-07: the migration script now treats orphans as a valid migration source (`tradeRole='BUYER'` default, `legacyUserId=null`, tallied as `migrated_orphan`). After re-run, all 12 dev MasterAccounts are in `better_auth_user`, including `ultrasooqdev@gmail.com`, `ultrasooq@gmail.com`, `u067788@gmail.com`, `ultrasooqtestdev@gmail.com`, `myphone.2day@gmail.com`, `pq.mct.mafwr@gmail.com`.
- **`auth.ts` runs at module load — `process.env.DATABASE_URL` is `undefined` unless dotenv is imported.** Nest's `ConfigModule` only loads `.env` after `NestFactory.create()`, but `auth.ts` is imported synchronously from the top of `main.ts`. Without `import 'dotenv/config'` at the top of `auth.ts`, the PrismaPg adapter silently falls back to defaults, leading to "table public.better_auth_user does not exist" errors on any sign-in/sign-up. Fixed by adding the dotenv import.
